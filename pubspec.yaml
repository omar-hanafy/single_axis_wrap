name: single_axis_wrap
description: A Flutter widget that chooses one complete row or column based on available space.
version: 1.0.3-dev.2
repository: https://github.com/omar-hanafy/single_axis_wrap
homepage: https://github.com/omar-hanafy/single_axis_wrap
topics:
  - layout
  - responsive
  - wrap
  - single
  - adaptive

environment:
  sdk: ^3.12.0
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
